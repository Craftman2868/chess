palettes:
  - name: global_palette
    images: automatic

converts:
  - name: sprites
    palette: global_palette
    transparent-color-index: 0
    tilesets:
      tile-width: 22
      tile-height: 22
      images:
        - chess.png

outputs:
  - type: c
    include-file: gfx.h
    palettes:
      - global_palette
    converts:
      - sprites
